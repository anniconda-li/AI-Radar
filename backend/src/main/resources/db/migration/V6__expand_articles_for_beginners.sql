UPDATE articles
SET content_markdown = CONCAT(
'## 先用最通俗的话理解 DeepSeek Chat

你可以把它理解成一个“能阅读你输入的文字，并根据上下文继续回答的助手”。背后运行的是大语言模型：它不是在数据库里查出一条固定答案，而是根据训练中学到的语言规律和你当前提供的材料生成回答。因此它很擅长解释、改写、归纳和提出思路，但也可能把不确定的内容说得很像真的。

打开网页以后，你会看到一个类似聊天软件的输入框。你输入问题，浏览器把文字发送给 DeepSeek 的服务器，模型生成回答后再传回页面。整个过程不要求你懂编程，也不会在你的电脑上安装模型。

## 零基础用户可以从这些例子开始

- 把一段难懂的新闻贴进去，让它用初中生能听懂的话解释，并列出文中的专业词。
- 上传一份允许上传的课程材料，让它先列目录，再按目录逐节讲解。
- 提供报错和相关代码，让它解释报错出现在哪一步，而不是直接要求“帮我修好”。
- 写邮件或报告前，让它先整理提纲，再根据你确认的提纲生成正文。

如果回答不对，不需要换一种“神奇提示词”。先指出具体哪句话有问题，补充缺少的背景，或者要求它只依据你给出的材料重新回答。

## 它不是搜索引擎，也不是专业人员

搜索引擎主要帮助你找到网页，聊天模型主要帮助你理解和组织内容。模型可能不知道刚发生的事情，也无法保证引用真实存在。涉及金钱、健康、合同、政策和重要技术决策时，把它当成帮助你准备问题的工具，而不是最后拍板的人。

---

', content_markdown,
'

## 常见困惑

**为什么同一个问题两次回答不完全一样？** 模型生成具有一定随机性。重要任务要固定材料、要求结构化输出，并人工比较结果。

**为什么上传长文件后会漏内容？** 文件解析、上下文长度和问题范围都会影响结果。先让它说明文件结构，再缩小到具体章节。

**是否必须注册？** 具体体验和额度以当前页面为准。注册只是使用产品账号，不等于获得开放平台 API Key。')
WHERE slug = 'deepseek-chat-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## API 是什么，为什么普通聊天不需要它

API 可以理解成“程序之间约定好的办事窗口”。人使用 DeepSeek Chat 时，是在网页里输入文字；程序使用开放平台时，则按照文档规定的地址、请求格式和身份凭证自动发送数据。只有当你准备写程序、做网站或批量处理任务时才需要 API。

API Key 类似一张带余额的门禁卡。服务器看到它，才知道调用属于哪个账户、应该扣谁的额度。任何拿到这张 Key 的人都可能消耗你的余额，因此它不能出现在前端代码、截图和 Git 仓库中。

## 一次请求实际经历了什么

用户在网页提交文字后，你自己的 Spring Boot 后端先检查用户和参数；后端从服务器环境变量读取 Key，再调用 DeepSeek；模型结果回到后端，后端记录用量、过滤错误并返回 Vue。把 Key 放在后端，就是为了让浏览器用户永远看不到它。

## 先认识命令里的几个词

- `curl.exe` 是发送 HTTP 请求的命令行工具。
- URL 是请求要到达的服务地址。
- `Content-Type: application/json` 告诉服务器，请求正文采用 JSON 格式。
- `Authorization: Bearer ...` 携带 API Key 证明身份。
- `model` 指定要调用的模型 ID。
- `messages` 是本次对话内容。

Token 不是汉字数，也不是请求次数，而是模型切分文字后的计费单位。输入和输出通常分开计价，所以限制无关上下文和最大输出能够直接控制费用。

---

', content_markdown,
'

## 每一步为什么这样做

先在当前 PowerShell 窗口设置环境变量，是为了避免把密钥写进脚本文件；关闭窗口后该变量就消失。随后用最小请求验证，是为了一次只排查账户、网络、密钥和模型名四件事。最小请求成功后再写 Java 代码，否则你会同时面对 SDK、配置、业务逻辑和网络错误。

## 常见错误如何判断

- **401**：服务器没有认可身份，先检查 Key 是否复制完整、是否属于正确平台。
- **400**：JSON、模型 ID 或参数不符合要求，先回到最新官方示例逐项比较。
- **429**：调用太快或达到并发限制，应降低频率并做退避重试。
- **连接超时**：先检查网络、代理和域名解析，不要马上重新生成 Key。
- **返回成功但费用过高**：检查是否带入了过长历史消息，以及最大输出是否没有限制。')
WHERE slug = 'deepseek-api-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## Kimi 到底是一款什么产品

对零基础用户来说，Kimi 首先是一个可以直接对话的网页应用；进一步使用时，它又像一个能够把资料加工成报告、表格、演示和网页的工作台。“Agent”这个词表示它不只回答一句话，还可能围绕目标连续执行搜索、整理和生成多个步骤。

你不需要先挑模型，也不需要配置 API。打开产品、登录账号、输入任务，就是最简单的使用方式。只有当你要操作本地代码、连接桌面文件或让自己的程序自动调用时，才需要 Kimi Code、Kimi Work 或开放平台。

## 一个完整任务是什么样的

假设你想比较六家 AI 厂商。不要只写“帮我比较”。可以说明只使用官网资料，需要厂商、产品、模型、价格、更新时间和来源六列，最后生成可编辑表格。Kimi 会尝试检索资料、整理字段和生成结果，你要检查每个来源是否真的支持对应单元格。

生成演示或文档也是同样思路：先给受众、目的、页数和必须包含的内容，再让它做第一版。AI 生成第一版很快，但最终结构、事实和表达仍需要人决定。

## 常见功能之间的关系

深度研究负责查找和综合资料；Docs、Sheets、Slides 是输出形态；网站生成把内容组织成网页；这些都是 Kimi 工作空间内的能力，不代表每个功能都需要单独安装。

---

', content_markdown,
'

## 零基础使用顺序

1. 先做无需上传私密资料的小任务。
2. 检查回答中的事实和来源，了解它会怎样出错。
3. 再尝试文件、表格和演示等复杂任务。
4. 导出文件后打开检查格式、公式、链接和引用。

如果某个任务反复失败，先缩小范围。例如把“完成整份行业报告”拆成“确定目录、搜集来源、整理表格、撰写章节、最终校对”。')
WHERE slug = 'kimi-workspace-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 先理解“桌面 Agent”这个概念

普通网页助手只知道你主动输入或上传的内容；桌面 Agent 在得到许可后，可以接触本地文件、桌面应用和持续任务。它更像一个在电脑上工作的助手，所以能力更强，权限风险也更大。

Kimi Work 适合资料大量存在本机、任务需要反复执行或需要多个 Agent 分工的知识工作。它不是“装上以后电脑自动变聪明”，而是提供一个能够连接资料与工具的工作环境。

## 它可能怎样帮助你

例如每周需要汇总几十份公开报告：你可以把副本放进专用目录，让它提取固定字段、记录来源并生成周报。又例如需要持续跟踪公开网站，可以让计划任务定时收集变化，但发送报告、覆盖文件等动作仍应由人确认。

## 为什么权限设置比提示词更重要

如果授权整个磁盘，Agent 可能看到与任务无关的照片、密钥和工作文件。正确做法是创建最小工作目录，只复制允许处理的资料；连接邮箱、云盘或企业系统时也先选择只读权限。

---

', content_markdown,
'

## 第一次使用的安全清单

- 使用测试目录和资料副本，不直接处理唯一原件。
- 关闭不需要的本地文件、邮件和网页账号。
- 查看每一个数据源请求的权限范围。
- 让自动化先生成草稿，不自动发送或发布。
- 记录任务执行时间、输入来源和生成文件位置。

如果产品版本、安装入口和系统支持发生变化，从 Kimi 官方产品页进入，不执行来源不明的第三方安装包。')
WHERE slug = 'kimi-work-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 在安装前，先认识你要安装的东西

Kimi Code 不是一个新的编程语言，也不是把大模型下载到电脑里。它是一个命令行程序：你在终端输入 `kimi`，这个程序读取当前项目中允许访问的文件，把任务和相关代码发送给模型，再根据你的确认修改文件或运行命令。

“命令行”“终端”“Shell”常常一起出现。终端是输入命令的窗口；Shell 负责解释命令；CLI 表示通过命令行操作的软件。Windows 上 Kimi Code 使用 Git Bash 作为执行环境，所以即使你平时使用 PowerShell，也需要 Git for Windows 提供这套 Shell。

## 它能做什么

它可以解释项目结构、搜索代码、修改多个文件、运行测试、分析报错和生成提交说明。它最有价值的地方是能看到整个仓库并连续完成步骤，而不是只生成一小段孤立代码。

它也可能误解需求、修改范围过大或执行不合适的命令。因此 Git、测试和人工审查不是附加功能，而是使用代码 Agent 的安全网。

## 安装脚本会做什么

官方脚本通常会判断操作系统和 CPU 架构，下载对应的 Kimi Code 可执行文件，把它放到用户可执行目录，并更新 PATH，让新终端能够找到 `kimi` 命令。运行远程脚本等于允许脚本在当前用户权限下执行操作，所以必须确认域名和官方文档。

---

', content_markdown,
'

## 逐步理解安装过程

1. `git --version` 只读取版本，不会修改电脑。失败说明 Git 尚未安装或 PATH 未生效。
2. `irm ...` 从官方地址下载脚本文本；管道后的 `iex` 执行这段文本。因此域名错误时不要继续。
3. 安装完成后重开终端，是为了让新窗口读取更新后的 PATH。
4. `kimi --version` 只验证命令能否启动，不会读取项目。
5. `cd` 切换当前目录；随后运行 `kimi`，这个目录就成为 Agent 观察项目的起点。
6. `/login` 是 Kimi Code 程序内部命令，不是在 PowerShell 中执行。

## 常见错误和处理顺序

**提示找不到 kimi：** 先重开终端，再检查安装输出中写入的目录是否已经进入 PATH，不要立刻重复安装。

**PowerShell 禁止运行脚本：** 阅读完整错误，确认公司策略是否允许；不要为了一个工具盲目把系统执行策略改成最宽松。

**网络下载失败：** 检查代理、DNS 和官方域名。重复执行不会解决被拦截的网络。

**进入程序后无法登录：** 区分 Kimi 网页账号、Code 订阅和开放平台 Key，确认选择了真实购买的服务。

## 完成安装后的第一个任务

先创建 Git 提交或备份，然后要求它只读分析。确认它能说清楚启动命令和测试命令后，再让它修改一个小文件。修改完成必须查看 diff 和运行测试；不要用“看起来没报错”代替验证。')
WHERE slug = 'kimi-code-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 为什么 Agent 需要浏览器扩展

网页不是一份简单文本：按钮、表格、分页和登录状态都需要浏览器交互。普通模型只能根据给定内容回答，WebBridge 则把“看见页面和操作页面”的能力连接给 Agent。

例如比较多个商品时，Agent 可能需要打开页面、选择地区、翻页并提取字段。扩展充当浏览器与 Agent 之间的桥梁，所以名称中使用了 WebBridge。

## 它能看到什么取决于权限

浏览器扩展会声明能够访问哪些网站、标签页或浏览器功能。权限越广，自动化越方便，但能接触的数据也越多。安装前查看发布者和权限说明；任务结束后关闭不再需要的站点权限。

## 什么任务适合自动浏览

公开信息收集、重复页面读取和无需登录的表格整理风险相对低。涉及账号、发布、支付、上传、删除和权限修改时，不应该让 Agent 无人值守地完成最后一步。

---

', content_markdown,
'

## 新手验证方法

先选择一个公开网页，让它只读取页面标题和三条可见信息。确认无误后再增加一次点击，最后才尝试跨页面流程。每增加一种动作，都要知道失败时页面会停在哪里。

如果扩展无法操作页面，先检查当前网站权限、页面是否在受支持范围、浏览器是否阻止扩展，而不是连续重复点击。网站更新布局后，旧步骤也可能需要重新配置。')
WHERE slug = 'kimi-webbridge-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 先理解开放平台解决的是什么问题

Kimi 网页适合人直接使用；Kimi API 适合程序自动使用。例如你的网站收到一段用户文本，需要自动总结并把结果返回，这个过程不可能让开发者每次手工打开 Kimi，因此需要 API。

开放平台提供模型、调用地址、API Key、用量和账单。你写的程序负责业务规则，Kimi 模型负责理解和生成，两者通过 HTTP 和 JSON 交换数据。

## 零基础需要认识的词

- **HTTP 请求**：程序通过网络向服务发送的一次消息。
- **JSON**：用键和值表达数据的文本格式。
- **API Key**：识别调用账户的秘密凭证。
- **Base URL**：API 服务的基础地址。
- **Model ID**：程序选择具体模型时使用的精确名称。
- **流式输出**：结果不是等待全部生成完再返回，而是一小段一小段到达。

## K2.6 和 K2.7 Code 怎么选

如果任务主要是通用问答、图片或视频理解，从 K2.6 开始；如果任务是代码 Agent、跨文件修改和软件工程，查看 K2.7 Code。不要只追求最新型号，应在自己的真实样本上比较质量、速度和费用。

---

', content_markdown,
'

## 理解示例命令

设置环境变量只是把 Key 临时保存在当前终端内存中。`curl.exe` 向 API 地址发送 JSON；请求头告诉平台内容格式并携带身份；请求正文选择模型并给出用户消息。

最小请求成功后，下一步不是立刻上线，而是把调用放进自己的后端，增加参数校验、超时、最大输出、日志和费用限制。前端永远不应该直接持有 Key。

## 新手常见误区

网页会员并不自动包含 API 额度；Kimi Code 订阅也不等于开放平台余额。模型名称必须与文档完全一致。图片和视频输入还会涉及文件格式、大小和上传方式，先让纯文本请求成功再增加多模态。')
WHERE slug = 'kimi-api-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## Z.ai 是模型还是软件

GLM 是智谱的模型系列，Z.ai 是让普通用户直接使用这些能力的产品界面。就像浏览器不是互联网本身，Z.ai 也不是模型文件；它负责账号、对话、上传材料和结果展示。

你不需要选择服务器、申请 Key 或写代码。打开页面输入任务即可。模型在云端运行，电脑主要负责显示页面和上传你主动选择的内容。

## 新手适合做的三类任务

第一类是解释：把专业材料变成分层说明，并让它列出术语。第二类是整理：从给定内容提取字段、时间线和争议点。第三类是创作：根据明确受众和结构生成初稿，再由人修改。

代码辅助也可以从解释报错和阅读函数开始。只有需要 Agent 长时间修改仓库时，才考虑 ZCode 或 Coding Plan 工具。

---

', content_markdown,
'

## 怎样判断回答是否可信

要求它标明哪些结论来自你提供的材料，哪些是推断。随机抽查数字、日期和引用；无法打开的来源不作为依据。对于自己不了解的主题，可以先让它给出核对清单，而不是直接接受结论。

如果回答过于专业，明确说“假设读者从未学过 AI，先解释术语，再举生活例子”。这是正常的任务要求，不需要掌握专门提示词。')
WHERE slug = 'z-ai-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## ZCode 和聊天助手到底差在哪里

聊天助手通常只看到你粘贴的一小段代码；ZCode 面向整个软件仓库，可以搜索文件、理解模块关系、制定修改计划并运行开发命令。它更接近“会操作项目的协作者”，而不是“回答代码问题的聊天框”。

## 一个真实工作过程

你提出“给后端增加分页查询”后，它可能先搜索 Controller、Service 和 Repository，阅读现有测试，列出计划，修改多个文件，再运行 Maven 测试。如果测试失败，它还会继续读取错误并修正。

每一步都可能影响真实文件，所以开始前要让 Git 工作区清晰。Git 不是为了方便 Agent，而是为了让你知道它改了什么并能安全撤销。

## 它适合谁

最好由已经知道项目如何启动和测试的人使用。初学者也能用，但必须先理解目录范围、差异检查和命令确认，否则容易在不明白的情况下接受大范围修改。

---

', content_markdown,
'

## 授权命令前问自己三件事

这条命令会读取还是写入？影响范围是否只在当前项目？失败后是否可以通过 Git 或备份恢复？删除文件、修改数据库、安装系统软件和上传代码都需要更谨慎。

如果 Agent 一开始就提出大范围重构，先要求它解释原因、列出具体文件并拆成小步骤。高质量开发流程依靠可验证的小改动，而不是一次生成大量代码。')
WHERE slug = 'zcode-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## Coding Plan 不是一个代码编辑器

购买 GLM Coding Plan 后，你得到的是模型调用套餐和专用凭证；真正显示代码、读取仓库和执行命令的是 Claude Code、OpenCode、Cline、Cursor 或 ZCode 等工具。套餐像通信流量，编程工具像手机，两者缺一不可。

## 为什么需要 Node.js 和 npx

官方配置助手使用 JavaScript 编写，需要 Node.js 运行。npm 是随 Node.js 提供的包管理器，npx 可以临时下载并执行一个工具，而不要求你先全局安装。`node --version` 和 `npm --version` 都只是读取版本，不会修改项目。

`npx @z_ai/coding-helper` 会下载官方配置助手并运行向导。向导通常帮助你选择编程工具、写入对应配置文件、设置专用端点和密钥。因为它会修改工具配置，所以运行前关闭正在使用相关配置的编辑器，并记住原配置位置。

## 为什么不能把套餐 Key 当普通 API

Coding Plan 的价格和额度建立在交互式编程使用场景上。把它放进网站后端或批处理脚本会违反使用范围，也可能导致凭证被限制。自建应用应申请开放平台通用 Key。

---

', content_markdown,
'

## 配置向导每一步在做什么

1. 检查 Node/npm，是为了确认助手能运行。
2. 准备套餐密钥，是为了让助手写入正确凭证，而不是普通 API Key。
3. 选择工具，是因为不同工具的配置文件和协议不同。
4. 运行 doctor，是为了检查配置文件、端点和命令是否可用。
5. 用测试仓库验证，是为了避免首次配置直接影响重要代码。

## 常见问题

如果 npx 无法下载，检查 npm 网络和代理；如果工具仍提示未认证，重启工具并确认它读取的是哪一个配置文件；如果产生意外按量账单，检查是否误用了通用 Key 和普通 Base URL。')
WHERE slug = 'glm-coding-plan-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 开放平台到底是什么

Z.ai 是给人使用的网页，智谱开放平台是给开发者使用的控制台和 API 服务。你可以在控制台创建 Key、查看模型、测试请求和查询账单，也可以让自己的后端通过网络自动调用 GLM。

“开放”不表示免费或不受限制，而是平台把模型能力通过文档化接口提供给开发者。每次请求仍需要身份、额度，并受到模型参数、内容规则和并发限制。

## 模型、知识库和 Agent 的关系

模型负责理解和生成；知识库先从企业文件中找到相关段落，再把段落交给模型回答；Agent 在模型判断下选择工具并连续执行步骤。初学者应从单一模型请求开始，因为同时引入三层后，很难知道错误发生在哪里。

例如做内部制度问答：先确认模型 API 能回答普通问题，再导入少量公开制度测试检索，最后才增加多轮对话和工具。每一步都要有能够人工判断的测试问题。

## 调用为什么必须经过自己的后端

浏览器代码会下载到用户电脑，任何写在 Vue 中的 Key 都能被看到。Spring Boot 后端则运行在你控制的服务器，可以从环境变量读取 Key、检查用户权限并限制费用。

---

', content_markdown,
'

## 逐行理解最小请求

环境变量把密钥临时放在终端；URL 指向智谱兼容聊天接口；两个请求头分别说明 JSON 格式和账户身份；正文中的 `model` 必须是平台当前存在的精确 ID，`messages` 则描述对话角色和内容。

请求成功后先保存一份原始 JSON，认识 `choices`、`usage` 和错误结构，再引入 Java SDK。这样将来遇到问题时，你知道是 HTTP 层、SDK 层还是业务层。

## 常见错误顺序

先检查区域和账号，再检查 Key，再核对模型 ID与请求 JSON，最后才怀疑 SDK。知识库效果差时先查看检索出的原文片段，不要只调整模型提示词。')
WHERE slug = 'zhipu-api-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 通义适合完全不懂 AI 的用户吗

适合。它的基础使用方式与聊天软件接近：输入自然语言，等待回答，再根据结果继续追问。你不需要知道模型参数、Token 或 API，只需要能把自己的目标和材料说清楚。

## 它常见的功能可以怎样理解

问答是让 AI 解释或提供思路；搜索是结合公开网页整理信息；创作是按照要求生成文字；文件处理是对你上传的材料做摘要、提取或改写。这些功能可能在不同版本和端上有不同入口，但背后都需要你明确任务并核对结果。

## 一个新手任务的完整示例

假设你要写一封课程请假邮件。先告诉它收件人、原因、日期和语气，要求不虚构额外信息。得到草稿后检查姓名、时间和事实，再自己发送。AI 负责起草，不应该替你决定事实或自动发送。

处理陌生主题时，可以先让它解释关键词、给出学习顺序和需要核对的问题，再逐个学习。不要第一次提问就要求生成最终专业结论。

---

', content_markdown,
'

## 为什么有时回答看起来懂了却不可靠

语言模型的目标是生成合理文字，不是保证每句话都来自数据库。它可能混淆相近产品、旧版本和新价格。看到数字、日期、引用和专业建议时，进入来源页面检查。

## 从通义转向开发工具的判断

只想自己问问题，用通义；希望 AI 在本地代码仓库中工作，用 Qwen Code；希望自己的程序自动调用模型，用阿里云百炼。选择入口前先问“是人直接使用，还是程序自动使用”。')
WHERE slug = 'tongyi-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## Qwen Code 是什么，安装后会发生什么

Qwen Code 是命令行代码 Agent。安装不会把大型模型下载到本机，它会安装一个较小的客户端程序；客户端读取当前项目、通过网络请求模型，并在你确认后修改文件或执行命令。

它可以帮助搜索代码、解释项目、实现功能、修改多处文件和运行测试。它不知道你心里真正想要什么，所以需求越模糊，修改范围越可能偏离。

## 为什么 Windows 特别标明“管理员 CMD”

CMD 和 PowerShell 是两种不同 Shell，环境变量和命令语法不同。官方 Windows 命令使用 `%TEMP%` 和 `&&`，这是 CMD 语法；直接粘贴到 PowerShell 会被不同方式解释。管理员权限可能用于把命令放入公共可执行位置或更新 PATH，因此运行前应确认命令来自官方文档。

下载命令分两步：`curl` 把官方批处理文件保存到临时目录；`&&` 表示只有下载成功才运行脚本；`--source bailian` 告诉安装器采用百炼来源配置。macOS/Linux 命令则下载 Shell 脚本并用 Bash 执行。

## `/auth` 为什么不能在 PowerShell 中运行

`/auth` 是 Qwen Code 程序内部命令。先在系统终端运行 `qwen` 进入程序，再输入 `/auth`。它用于选择按量 API、Coding Plan 或 Token Plan，并把对应凭证写入工具配置。

---

', content_markdown,
'

## 安装每一步的目的

1. 准备账号和方案：决定后面使用哪一种 Key，避免额外计费。
2. 下载官方安装器：取得与你系统匹配的客户端。
3. 重开终端：让系统重新读取 PATH。
4. 查看版本：确认安装文件能运行。
5. 启动并认证：让客户端获得模型调用权限。
6. 进入项目：限定 Agent 可以观察和修改的范围。

## 常见错误

找不到 `qwen` 时先重开终端和检查 PATH；`/auth` 失败时确认方案与 Key 类型；模型不可用时核对套餐允许的精确模型名；安装器下载失败时检查代理和官方域名，不要从陌生网盘下载替代程序。

第一次任务只让它解释项目，不修改。接着做一个可通过 Git 撤销的小改动，检查 diff 和测试后再逐步扩大范围。')
WHERE slug = 'qwen-code-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 百炼不是一个模型，而是一座“模型工作台”

通义千问是模型与产品品牌，阿里云百炼是让开发者选择、调用和管理模型的平台。平台中既可能有阿里自己的模型，也可能提供第三方模型。你在这里创建 Key、查看用量、构建知识库或智能体。

## 初学者先认识五个概念

- **模型**：负责理解输入并生成结果。
- **API**：程序调用模型的统一入口。
- **工作空间**：团队资源、Key 和权限所在的范围。
- **知识库**：把文档切分和索引，在回答前检索相关内容。
- **智能体**：让模型根据任务调用工具并执行多个步骤。

区域非常重要。中国内地和国际站可能使用不同控制台、Base URL、模型和价格。资源建在哪个区域，程序就要使用对应的地址和凭证。

## 一个后端调用怎样流动

Vue 把用户输入发给 Spring Boot；后端验证用户并从环境变量读取 `DASHSCOPE_API_KEY`；后端请求百炼；结果返回后端，后端记录 Token 和耗时再返回网页。任何让 Vue 直接请求百炼的做法都会暴露 Key。

---

', content_markdown,
'

## 为什么要先做最小请求

最小请求只验证账号、Key、区域、端点和模型 ID。它成功后再加入 SDK、流式输出、知识库和业务数据库。一次增加一个变量，错误才容易定位。

示例中的环境变量仅在当前 PowerShell 窗口有效；关闭窗口不会把 Key留在代码中。curl 请求头携带身份，正文选择模型并传递消息。模型 ID 更新时，复制当前官方文档而不是猜名称。

## 上线前还缺哪些东西

开发示例只证明“能调用”，生产系统还要有超时、重试、限流、最大输出、日志、预算告警和权限。启用搜索、代码解释器或 MCP 后，除了模型 Token 还可能产生工具费用。')
WHERE slug = 'model-studio-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 先用手机套餐类比 Coding Plan

按量 API 像按实际通话时长收费；Coding Plan 更像固定月费、带周期额度的套餐。它不是把一笔人民币余额放进账户，而是允许受支持的交互式工具在规则内调用指定模型。

## 为什么 Key 和地址必须专用

平台通过 Key 前缀和 Base URL 判断这次请求属于套餐还是普通按量服务。使用 `sk-sp-` 专用 Key 并访问包含 `coding` 的地址，才会按照 Coding Plan 处理；误用普通 `sk-` Key 会进入按量计费。

## 一次提问为什么可能消耗多次调用

代码 Agent 收到任务后，可能先规划、读取文件、生成修改、检查错误再修复，每一步都可能调用模型。因此套餐中的“请求次数”不等于你在聊天框按下回车的次数。复杂任务消耗通常更高。

## 它适合什么人

适合每天在受支持工具中交互式编程的个人用户。团队共享、自建网站、自动批处理和无人值守服务不属于同一场景，应使用企业或通用 API 方案。

---

', content_markdown,
'

## 配置时逐项确认

先在套餐页复制专用 Key，再根据工具选择 OpenAI 或 Anthropic 兼容地址。配置文件可能保存在用户目录，修改后要重启工具。用一个测试仓库提简单问题，并在套餐页面观察额度是否变化，以确认没有走到普通账单。

## 出现意外费用时怎么查

先确认工具当前选中的供应商和模型，再检查 Key 前缀、Base URL 是否包含 `coding`，最后查看是否同时保存了普通百炼配置。有些工具会自动选择其他提供商，删除不需要的旧配置比反复充值更重要。')
WHERE slug = 'bailian-coding-plan-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 豆包是什么，为什么不需要懂模型

豆包是已经包装好的个人 AI 应用。模型、服务器和接口由产品处理，用户只需要通过网页、电脑客户端或手机应用输入任务。你不需要购买云服务器，也不需要自己安装大模型。

## 它能帮助普通用户做什么

问答可以解释概念；搜索可以整理公开信息；创作可以生成文字或提供表达建议；多模态表示它还能处理图片、语音等不只纯文字的输入。具体能力会随版本和终端变化。

例如准备旅行计划时，可以让它先列出需要确认的日期、预算、交通和证件，再根据你提供的条件整理方案。真正的票价、开放时间和政策仍要进入官方渠道确认。

## 客户端权限代表什么

桌面或移动应用可能请求麦克风、相册、文件和通知权限。麦克风用于语音，文件权限用于上传材料；不使用的功能无需授权。权限可以在系统设置中随时检查和关闭。

---

', content_markdown,
'

## 如何与 AI 对话而不是“碰运气”

先说明你是谁、要完成什么、有哪些已知条件和希望的结果形式。得到回答后指出具体需要修改的地方。复杂任务先要计划，再逐步执行；不要把几十个要求塞进一句话后期待一次完美。

## 为什么还需要人工检查

AI 可能使用过时资料、误读图片或编造来源。对日期、金额、地址、链接和专业建议逐项核对。发布内容前还要检查隐私、版权和是否符合实际情况。')
WHERE slug = 'doubao-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 火山方舟和豆包不是同一个东西

豆包是普通用户直接使用的应用；火山方舟是开发者和企业使用的模型服务平台。你可以把豆包理解成成品工具，把方舟理解成提供发动机、测试台和维护工具的开发平台。

## 推理、评测和精调分别是什么

推理就是把输入交给已存在的模型并得到输出；评测是用一组固定样本比较质量、速度和成本；精调是使用特定数据调整模型行为。大多数项目应先调用现有模型和做评测，不要一开始就投入精调。

## 为什么有时需要“接入点”

云平台可能要求先把模型以某种配置部署或创建推理接入点，程序实际调用接入点 ID。它类似为你的项目创建一个固定服务入口，因此别人的 ID 或旧教程中的 ID 不能直接复制。

## 不同模态怎样计费

文本常按 Token；图片可能按张数、分辨率或处理量；视频可能按时长和规格。比较价格前必须统一模型版本、输入长度和媒体规格，仅看一个数字没有意义。

---

', content_markdown,
'

## 新手接入顺序的理由

先选模型是为了确定输入格式和价格；创建最小权限凭证是为了控制泄露影响；使用官方示例是为了避免过期端点；先做非流式请求是为了得到完整易读的错误；最后再增加图片、视频和工具。

## 常见问题

认证失败时检查账号、区域和权限；找不到模型时检查模型是否在当前区域开放；请求成功但没有预期效果时先构建几条可判断对错的评测样本；费用异常时检查是否使用了高规格媒体或过长上下文。')
WHERE slug = 'volcengine-ark-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## Agent 是什么，为什么模型本身还不够

普通模型接收一次输入并生成一次输出。Agent 在模型外增加了计划、工具、记忆和循环：模型决定下一步，系统调用工具，把结果再交给模型，直到完成任务或达到限制。

例如“根据库存生成补货建议并发给负责人”不仅需要写文字，还要读取库存 API、计算规则、生成报告和发送消息。每个外部动作都需要权限、错误处理和审计，AgentKit 就是管理这些生产问题的平台。

## 用生活例子理解组件

模型像负责思考的人；工具像电话、浏览器和数据库；知识库像可查阅的资料柜；记忆像工作笔记；运行时像办公室；可观测系统像监控和工作记录；身份权限决定谁能打开哪一个柜子。

## 为什么不应一开始做多 Agent

多个 Agent 会增加通信、成本和错误路径。先让一个 Agent 使用一个只读工具完成小任务，能够解释并复现每一步后，再考虑分工。复杂架构不能代替清晰任务。

---

', content_markdown,
'

## 第一个练习项目

可以创建“公开资料摘要 Agent”：输入一个公开页面，工具只允许读取，Agent 提取标题、日期和三条要点，最后列出来源。这个任务没有写操作，结果容易检查，适合验证模型、工具、日志和费用。

## 上线前必须设置的边界

限制最大步骤数和总费用；工具设置超时与参数校验；写操作要求人工确认；保存每一步输入输出；准备工具失败、恶意输入和模型跑偏的测试。没有这些保护，演示可用不等于生产可用。')
WHERE slug = 'agentkit-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 文小言可以理解成什么

它是百度面向个人用户的 AI 搜索和对话产品。普通搜索往往给出一组网页链接，AI 搜索会尝试阅读和归纳结果，再用自然语言回答。这样更省阅读时间，但也增加了归纳错误和来源遗漏的可能。

## 零基础用户怎样提问

不要只问“哪个好”。说明你的使用场景、预算、时间和必须满足的条件，再要求它按固定字段比较。学习陌生主题时，先让它解释术语和列出学习路径，不要直接要求给最终结论。

## 怎样检查搜索型回答

查看引用链接是否能打开，发布日期是否符合问题时间范围，原文是否真的支持回答。多个网站互相转载同一错误内容，并不等于有多个独立来源。

## 生成内容时人的职责

AI 可以起草标题、提纲和正文，但姓名、数字、日期、承诺和观点应由人确认。生成的文字可能很完整，却不知道你没有告诉它的真实背景。

---

', content_markdown,
'

## 一个安全的使用练习

选择一篇公开文章，让文小言分别生成一句话摘要、五条要点和面向小学生的解释。逐项对照原文，观察哪些内容准确、哪些被省略。这个练习能帮助你理解 AI 的优势和边界。

不要上传身份证、合同、账号凭证和未公开工作文件。需要企业数据接入、权限和自动化时，应使用百度千帆等开发平台。')
WHERE slug = 'wenxiaoyan-guide';

UPDATE articles
SET content_markdown = CONCAT(
'## 千帆是一套平台，不是一个聊天机器人

百度千帆面向开发者和企业，提供模型调用、Agent 开发、知识库、工具、评测和治理。普通用户只想聊天可以使用文小言；要把 AI 接进客服、内部知识问答或业务流程，才需要千帆。

## 用企业问答理解整个流程

员工在网页提问，后端验证身份；知识库从制度文件中检索相关段落；模型根据段落组织回答；系统把答案和来源返回员工。若问题还需要查询假期余额，Agent 会调用内部工具。每一层都承担不同职责。

## 初学者需要认识的术语

- **SDK**：厂商为某种编程语言准备的调用工具包。
- **鉴权**：证明请求来自哪个账号并检查权限。
- **RAG/知识库问答**：先检索资料，再让模型基于资料回答。
- **MCP**：让 Agent 用统一方式发现和调用工具的协议。
- **精调**：用特定数据进一步训练或调整模型，不等于上传几份文档。

## 为什么先做 API，再做 Agent

如果最基本的模型请求都不稳定，加入知识库和工具只会放大排查范围。先确认鉴权、模型和费用，再测试检索，最后组合成 Agent，是为了让每一层都能单独验证。

---

', content_markdown,
'

## 最小项目应该做到什么

后端使用环境变量保存凭证，发送一个短问题，打印模型回答和用量。成功后增加超时、错误分类和日志。再准备十条业务问题作为评测集，比较加入知识库前后的准确性。

## 生产环境要关注的不只是模型

企业系统还要处理账号权限、敏感数据、调用审计、预算、服务故障和内容安全。搜索、文档解析、模型、工具与部署资源可能分别计费，必须在真实用量下估算总成本。')
WHERE slug = 'qianfan-guide';
